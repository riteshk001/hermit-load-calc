[package]
name = "boot_image"
version = "0.1.0"
authors = ["Stefan Lankes <slankes@eonerc.rwth-aachen.de>"]
edition = "2021"
license = "MIT/Apache-2.0"
readme = "README.md"
keywords = ["operating systems", "kernel"]
categories = ["os"]
repository = "https://github.com/hermit-os/boot-image"
description = "Extend HermitOS` kernel to a common monolithic operting system."

[dependencies]
hermit-kernel = { path = "./kernel", default-features = false }
log = { version = "0.4.33", default-features = false }
goblin = { version = "0.10", default-features = false, features = ["elf32", "elf64", "endian_fd"] }
align-address = "0.4"
num = { version = "0.4", default-features = false }
embedded-io = { version = "0.7", features = ["alloc"] }
lz4_flex = { version = "0.13.1", default-features = false, features = ["safe-decode"] }

[features]
default = ["fork", "semihosting", "kernel-stack", "pci", "pci-ids", "fsgsbase", "tcp", "udp", "dns", "acpi", "common-os", "dhcpv4", "virtio-fs", "virtio-net"]
common-os = ["hermit-kernel/common-os"]
vga = ["hermit-kernel/vga"]
pci = ["hermit-kernel/pci"]
pci-ids = ["hermit-kernel/pci-ids"]
acpi = ["hermit-kernel/acpi"]
fsgsbase = ["hermit-kernel/fsgsbase"]
smp = ["hermit-kernel/smp"]
tcp = ["hermit-kernel/tcp"]
udp = ["hermit-kernel/udp"]
dns = ["hermit-kernel/dns"]
virtio-fs = ["hermit-kernel/virtio-fs"]
virtio-net = ["hermit-kernel/virtio-net"]
dhcpv4 = ["hermit-kernel/dhcpv4"]
kernel-stack = ["hermit-kernel/kernel-stack"]
semihosting = ["hermit-kernel/semihosting"]
fork = ["hermit-kernel/fork"]

[patch.crates-io]
# We need https://github.com/rust-osdev/volatile/pull/61
volatile = { git = "https://github.com/rust-osdev/volatile.git" }
